{
  "name": "mail-widgets",
  "private": true,
  "version": "0.1.0",
  "description": "Windows Gmail desktop widget",
  "author": {
    "name": "zoro-of-coding",
    "email": "zoro-of-coding@users.noreply.github.com"
  },
  "type": "module",
  "main": "electron/main.mjs",
  "scripts": {
    "dev": "electron .",
    "start": "electron .",
    "build": "electron-builder --publish never",
    "build:windows": "electron-builder --publish never --win nsis msi",
    "build:linux": "electron-builder --publish never --linux appimage deb",
    "build:macos": "electron-builder --publish never --mac dmg"
  },
  "devDependencies": {
    "electron": "^32.2.0",
    "electron-builder": "^24.13.3"
  },
  "optionalDependencies": {
    "keytar": "^7.9.0"
  },
  "build": {
    "appId": "com.zoroofcoding.mailwidgets",
    "productName": "Mail Widgets",
    "directories": {
      "buildResources": "build",
      "output": "release"
    },
    "files": [
      "electron/**/*",
      "src/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "config",
        "to": "config",
        "filter": [
          "google_oauth.json",
          "google_oauth.example.json"
        ]
      }
    ],
    "win": {
      "icon": "build/icon.ico",
      "target": [
        "nsis",
        "msi"
      ]
    },
    "linux": {
      "icon": "build/icon.png",
      "category": "Utility",
      "target": [
        "AppImage",
        "deb"
      ]
    },
    "mac": {
      "icon": "build/icon.png",
      "target": [
        "dmg"
      ]
    }
  }
}
